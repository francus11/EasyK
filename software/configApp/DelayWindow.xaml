<Window x:Class="configApp.DelayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Key Combination" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"  />
            <RowDefinition Height="1*
                           "  />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Width="173" VerticalAlignment="Bottom">
            <Label Content="Delay:" HorizontalAlignment="Right" VerticalAlignment="Center" />

            <TextBox x:Name="InputTextBox" TextChanged="InputTextBox_TextChanged" Height="18" VerticalAlignment="Center" Grid.ColumnSpan="2" Grid.Column="1" Width="131" />

        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0" Width="170" VerticalAlignment="Center">
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="0,0,10,0" IsEnabled="False" Click="OkButton_Click" />
            <Button Content="Cancel" Width="80" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>