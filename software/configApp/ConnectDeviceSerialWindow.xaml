<Window x:Class="configApp.ConnectDeviceSerialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connect to Device" Height="400" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Select Port:" VerticalAlignment="Center" Margin="5"/>
        <ComboBox x:Name="PortComboBox" Grid.Row="1" Margin="5" Height="25"/>

        <TextBlock Text="Send Data:" Grid.Row="2" VerticalAlignment="Center" Margin="5"/>
        <TextBox x:Name="SendTextBox" Grid.Row="3" Margin="5" Height="25"/>

        <Button x:Name="SendButton" Content="Send" Grid.Row="4" Width="75" Margin="5" HorizontalAlignment="Left" Click="SendButton_Click"/>

        <TextBlock Text="Received Data:" Grid.Row="5" VerticalAlignment="Top" Margin="5"/>
        <TextBox x:Name="ReceivedTextBox" Grid.Row="6" Margin="5" Height="100" VerticalScrollBarVisibility="Auto" IsReadOnly="True"/>

        <Button x:Name="Connect" Content="Connect" Grid.Row="2" Width="75" Margin="300,3,0,3" HorizontalAlignment="Left" Click="ConnectButton_Click"/>

    </Grid>
</Window>
